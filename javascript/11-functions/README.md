// Create a function called isOnline that accepts a boolean value (true or false).

// If the value is true, print "User is online".

// If the value is false, print "User is offline".


///////////////////////////////////////////////////////////////////////////////////////


// Create a function called userInfo that accepts a user's name and age.

// If the user is 18 or older, print "<name> is an adult".

// Otherwise, print "<name> is not an adult".
