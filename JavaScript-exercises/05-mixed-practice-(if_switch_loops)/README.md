## JavaScript Exercises – Mixed Practice (if, switch, loops)

### If Statements

1. Create a variable `number = 7`. Use if to check if the number is greater than 5 and print "Number is greater than 5".  
2. Create a variable `age = 16`. Use if to print "Minor" or "Adult".  
10. Create a variable `number = 10`. Use if to check if it is even or odd and print "Even" or "Odd".  
15. Create a variable `points = 85`. Use if to print:
   - "Excellent" for points >= 80  
   - "Good" for 60–79  
   - "Fail" for less than 60  

---

### For Loop + If

3. Create an array `grades = [5,3,4,2,5]`. Print only grades >= 4.  
4. Create an array `numbers = [1,2,3,4,5,6]`. Print only even numbers.  
5. Create an array `names = ["Ana","Marko","Jovan"]`. Print names starting with "A".  
6. Create an array `cities = ["Belgrade","Novi Sad","Niš"]`. Print cities with more than 5 characters.  
11. Create an array `results = [45,70,90]`. Print "Passed" or "Failed".  
14. Create an array `messages = ["Hello","World","JS"]`. Print messages that contain "o".  
21. Create an array `loggedIn = [true,false,true]`. Print "Logged in" or "Not logged in".  
23. Create an array `digits = [1,2,3,4,5]`. Sum numbers greater than 3.  
24. Create an array `grades = [5,3,4,2,5]`. Count how many grades are equal to 5.  
25. Create an array `cities = ["Belgrade","Novi Sad","Niš"]`. Check if it contains "Niš".  
26. Create an array `numbers = [1,2,3,4,5]`. Increase each number by 10.  
30. Create an array `grades = [5,3,4,2,5]`. Print:
   - "Excellent" for grades >= 5  
   - "Good" for grades 3–4  
   - "Fail" for grades < 3  

---

### Switch Statements

7. Create a variable `day = 3`. Print the name of the day (1 = Monday...).  
8. Create a variable `color = "green"`. Print:
   - "Go" (green)  
   - "Stop" (red)  
   - "Caution" (yellow)  
22. Create a variable `time = "night"`. Print:
   - "Good night"  
   - "Good morning"  
   - "Good day"  
27. Create a variable `type = "mobile"`. Print device type.  

---

### For + Switch

9. Create an array `statuses = ["online","offline","online"]`. Print user status.  
16. Create an array `roles = ["admin","user","guest"]`. Print role messages.  
20. Create an array `languages = ["JS","HTML","CSS"]`. Print language names.  

---

### Loops (for / while)

12. Create an array `numbers = [3,5,8,2]`. Sum all numbers.  
13. Create an array `numbers = [1,2,3,4,5]`. Find the largest number.  
17. Create an array `numbers = [2,4,6,8]`. Use while loop to print all numbers.  
18. Create a variable `x = 5`. Use while loop to decrease it to 0.  
19. Create an array `grades = [5,4,3,2,1]`. Calculate the average.  
28. Create an array `messages = ["Zdravo","Ćao","Hello"]`. Combine into one string.  
29. Create an array `numbers = [10,20,30]`. Divide each number by 2.  