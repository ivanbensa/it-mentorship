JavaScript Exercises – Mixed Practice (if, switch, loops)

1.

Create a variable number = 7.
Use if to check if the number is greater than 5 and print "Number is greater than 5".

2.

Create a variable age = 16.
Use if to print "Minor" or "Adult".

3.

Create an array grades = [5,3,4,2,5].
Use a for loop and if to print only grades greater than or equal to 4.

4.

Create an array numbers = [1,2,3,4,5,6].
Use a for loop and if to print only even numbers.

5.

Create an array names = ["Ana","Marko","Jovan"].
Use a for loop and if to print only names that start with the letter "A".

⸻

6.

Create an array cities = ["Belgrade","Novi Sad","Niš"].
Use a for loop and if to print cities with more than 5 characters.

7.

Create a variable day = 3.
Use switch to print the name of the day (1 = Monday, 2 = Tuesday, 3 = Wednesday…).

8.

Create a variable color = "green".
Use switch to print "Go" if it is green, "Stop" if it is red, "Caution" if it is yellow.

9.

Create an array statuses = ["online","offline","online"].
Use for + switch to print "User active" or "User not active".

10.

Create a variable number = 10.
Use if to check if it is even or odd and print "Even" or "Odd".

⸻

11.

Create an array results = [45,70,90].
Use for + if to print "Passed" for values greater than or equal to 60, "Failed" for others.

12.

Create an array numbers = [3,5,8,2].
Use a for loop to sum all numbers and print the result.

13.

Create an array numbers = [1,2,3,4,5].
Use a for loop to find the largest number.

14.

Create an array messages = ["Hello","World","JS"].
Use a for loop to print messages that contain the letter "o".

15.

Create a variable points = 85.
Use if to print "Excellent" for points >= 80, "Good" for 60–79, "Fail" for less than 60.

⸻

16.

Create an array roles = ["admin","user","guest"].
Use for + switch to print "Admin access" for "admin", "User" for "user", "Guest" for "guest".

17.

Create an array numbers = [2,4,6,8].
Use a while loop to print all numbers.

18.

Create a variable x = 5.
Use a while loop to decrease x while it is greater than 0 and print each value.

19.

Create an array grades = [5,4,3,2,1].
Use a for loop to calculate the average grade and print the result.

20.

Create an array languages = ["JS","HTML","CSS"].
Use for + switch to print "JavaScript", "HTML language", "CSS language" for each element.

⸻

21.

Create an array loggedIn = [true,false,true].
Use for + if to print "Logged in" or "Not logged in".

22.

Create a variable time = "night".
Use switch to print "Good night" if it is "night", "Good morning" if "morning", "Good day" if "day".

23.

Create an array digits = [1,2,3,4,5].
Use a for loop to sum all numbers greater than 3.

24.

Create an array grades = [5,3,4,2,5].
Use a for loop to count how many grades are equal to 5.

25.

Create an array cities = ["Belgrade","Novi Sad","Niš"].
Use for + if to check if the array contains "Niš" and print "Niš found" or "Niš not found".

26.

Create an array numbers = [1,2,3,4,5].
Use a for loop to increase each number by 10 and print the new array.

27.

Create a variable type = "mobile".
Use switch to print "Mobile device" for "mobile", "Desktop device" for "desktop".

28.

Create an array messages = ["Zdravo","Ćao","Hello"].
Use a for loop to combine all messages into one string and print the result.

29.

Create an array numbers = [10,20,30].
Use a for loop to divide each number by 2 and print the result.

30.

Create an array grades = [5,3,4,2,5].
Use a for loop and if to print "Excellent" for grades >= 5, "Good" for grades 3–4, "Fail" for grades less than 3.