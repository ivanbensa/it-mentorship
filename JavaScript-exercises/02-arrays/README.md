# JavaScript Exercises – ARRAYS

1. Create an array `numbers` with values `[1, 2, 3, 4, 5]`.
2. Print the entire `numbers` array to the console.
3. Print the first element of the `numbers` array.
4. Print the last element of the `numbers` array.
5. Create an array `names` with values `["Anna", "Marc", "James"]`.
6. Print the second element of the `names` array.
7. Change the value `"Marc"` to `"George"` in the `names` array.
8. Print the entire `names` array after the change.
9. Create an array `grades` with values `[5, 4, 3]`.
10. Print the sum of the first two grades.
11. Create an array `cities = ["Belgrade", "Novi Sad"]`.
12. Add `"Nis"` to the end of the `cities` array.
13. Print the `cities` array.
14. Remove the last element from the `cities` array.
15. Print the `cities` array after removing the last element.

---

## Combined Array Exercises

16. Create an array `numbers2 = [10, 20, 30]`.
17. Print the sum of all elements in the `numbers2` array (use indexes).
18. Create an array `prices = [100, 200, 300]`.
19. Increase the first price by 50 and print the new array.
20. Create an array `languages = ["HTML", "CSS", "Java"]`.
21. In the `languages` array, replace `"Java"` with `"JavaScript"`.
22. Print the `languages` array.
23. Create an array `statuses = [true, false, true]`.
24. Print the value of the second element in the `statuses` array.
25. Create an array `messages = ["Hello", "Hi"]` and print both messages one below the other.